 import { makeStyles } from "@material-ui/core";

 const useStyles = makeStyles((theme) => ({
    headerContainer: {
      background: "#be1622",
      height: "100%"
    },
  
    icon: {
      color: "#fff"
    },
  
  }));

  export default useStyles